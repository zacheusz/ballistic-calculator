# Snipe Ballistics API Configuration
# Copy this file to .env and fill in your API details

# Base API endpoint (without the stage)
VITE_API_ENDPOINT=https://api.calculator.snipe.technology

# API stage (e.g., test, dev, stage, prod)
VITE_API_STAGE=prod

# Note: Only variables prefixed with VITE_ are exposed to your Vite application
# The API key is handled separately through the application's configuration UI
API_KEY=your_api_key_for_playwright_mcp